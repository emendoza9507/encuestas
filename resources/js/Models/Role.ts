export interface Permission {
    id: string
    name: string
    guard_name: string
    created_at: string
    updated_at: string
}

export default interface Role {
    id: string
    name: string
    guard_name: string
    created_at: string
    updated_at: string
}
